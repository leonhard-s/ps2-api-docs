characters_currency:
  description: Description for `characters_currency`.
  properties:
    character_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      format: int64
      description: Description for `character_id`
      x-reference-to:
        - character.character_id
    currency_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `currency_id`
      x-reference-to:
        - currency.currency_id
    currency_name:
      type: string
      description: Description for `currency_name`
    quantity:
      type: integer
      description: Description for `quantity`
    prestige_currency:
      type: integer
      description: Description for `prestige_currency`
