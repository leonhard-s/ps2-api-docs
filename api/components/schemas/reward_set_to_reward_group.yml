reward_set_to_reward_group:
  description: Description for `reward_set_to_reward_group`.
  properties:
    reward_set_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `reward_set_id`
    reward_group_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `reward_group_id`
      x-reference-to:
        - reward_group_to_reward.reward_group_id
