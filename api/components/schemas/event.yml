event:
  description: Description for `event`.
  properties:
    character_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      format: int64
      description: Description for `character_id`
      x-reference-to:
        - character.character_id
    vehicle_definition_id:
      type: integer
      description: Description for `vehicle_definition_id`
    faction_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `faction_id`
      x-reference-to:
        - faction.faction_id
    attacker_player_guid:
      type: integer
      description: Description for `attacker_player_guid`
    attacker_character_id:
      type: integer
      minimum: 0
      format: int64
      description: Description for `attacker_character_id`
      x-reference-to:
        - character.character_id
    attacker_loadout_id:
      type: integer
      minimum: 0
      description: Description for `attacker_loadout_id`
      x-reference-to:
        - loadout.loadout_id
    attacker_weapon_id:
      type: integer
      minimum: 0
      description: Description for `attacker_weapon_id`
    attacker_vehicle_id:
      type: integer
      minimum: 0
      description: Description for `attacker_vehicle_id`
      x-reference-to:
        - vehicle.vehicle_id
    projectile_id:
      type: integer
      minimum: 0
      description: Description for `projectile_id`
      x-reference-to:
        - projectile.projectile_id
    timestamp:
      type: integer
      description: Description for `timestamp`
    zone_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `zone_id`
      x-reference-to:
        - zone.zone_id
    world_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `world_id`
      x-reference-to:
        - world.world_id
    event_type:
      type: string
      description: Description for `event_type`
    table_type:
      type: string
      description: Description for `table_type`
