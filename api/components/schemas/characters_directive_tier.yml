characters_directive_tier:
  description: Description for `characters_directive_tier`.
  properties:
    character_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      format: int64
      description: Description for `character_id`
      x-reference-to:
        - character.character_id
    directive_tree_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `directive_tree_id`
      x-reference-to:
        - directive_tree.directive_tree_id
    directive_tier_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `directive_tier_id`
      x-reference-to:
        - directive_tier.directive_tier_id
    completion_time:
      type: integer
      description: Description for `completion_time`
    completion_time_date:
      type: string
      description: Description for `completion_time_date`
