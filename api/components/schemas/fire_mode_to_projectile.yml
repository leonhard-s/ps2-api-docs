fire_mode_to_projectile:
  description: Description for `fire_mode_to_projectile`.
  properties:
    fire_mode_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `fire_mode_id`
      x-reference-to:
        - fire_mode.fire_mode_id
        - fire_mode_2.fire_mode_id
    projectile_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `projectile_id`
      x-reference-to:
        - projectile.projectile_id
