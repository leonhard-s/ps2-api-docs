character_name:
  description: Description for `character_name`.
  properties:
    character_id:
      type: integer
      format: int64
      x-summary: Summary for character_id
      description: Description for `character_id`
    name:
      type: object
      x-summary: Summary for name
      description: Description for `name`
      properties:
        first:
          type: string
        first_lower:
          type: string
