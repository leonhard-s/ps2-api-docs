outfit_member_extended:
  description: Description for `outfit_member_extended`.
  properties:
    character_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      format: int64
      description: Description for `character_id`
      x-reference-to:
        - character.character_id
    member_since:
      type: integer
      description: Description for `member_since`
    member_since_date:
      type: string
      description: Description for `member_since_date`
    member_rank:
      type: string
      description: Description for `member_rank`
    member_rank_ordinal:
      type: integer
      description: Description for `member_rank_ordinal`
    outfit_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `outfit_id`
      x-reference-to:
        - outfit.outfit_id
    name:
      type: string
      description: Description for `name`
    name_lower:
      type: string
      description: Description for `name_lower`
    alias:
      type: string
      description: Description for `alias`
    alias_lower:
      type: string
      description: Description for `alias_lower`
    time_created:
      type: integer
      description: Description for `time_created`
    time_created_date:
      type: string
      description: Description for `time_created_date`
    leader_character_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `leader_character_id`
      x-reference-to:
        - character.character_id
    member_count:
      type: integer
      description: Description for `member_count`
