world_event:
  description: Description for `world_event`.
  properties:
    facility_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `facility_id`
      x-reference-to:
        - map_region.facility_id
    faction_old:
      type: integer
      description: Description for `faction_old`
    faction_new:
      type: integer
      description: Description for `faction_new`
    duration_held:
      type: integer
      description: Description for `duration_held`
    objective_id:
      type: integer
      description: Description for `objective_id`
      x-reference-to:
        - objective.objective_id
    timestamp:
      type: integer
      description: Description for `timestamp`
    zone_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `zone_id`
      x-reference-to:
        - zone.zone_id
    world_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `world_id`
      x-reference-to:
        - world.world_id
    event_type:
      type: string
      description: Description for `event_type`
    table_type:
      type: string
      description: Description for `table_type`
    outfit_id:
      type: integer
      description: Description for `outfit_id`
      x-reference-to:
        - outfit.outfit_id
