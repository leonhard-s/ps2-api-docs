characters_stat:
  description: Description for `characters_stat`.
  properties:
    character_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      format: int64
      description: Description for `character_id`
      x-reference-to:
        - character.character_id
    stat_name:
      type: string
      description: Description for `stat_name`
    profile_id:
      type: integer
      description: Description for `profile_id`
      x-reference-to:
        - profile_2.profile_id
    value_forever:
      type: integer
      description: Description for `value_forever`
    value_monthly:
      type: integer
      description: Description for `value_monthly`
    value_weekly:
      type: integer
      description: Description for `value_weekly`
    value_daily:
      type: integer
      description: Description for `value_daily`
    value_one_life_max:
      type: integer
      description: Description for `value_one_life_max`
    last_save:
      type: integer
      description: Description for `last_save`
    last_save_date:
      type: string
      description: Description for `last_save_date`
