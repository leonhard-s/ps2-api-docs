objective_set_to_objective:
  description: Description for `objective_set_to_objective`.
  properties:
    objective_set_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      x-summary: Summary for objective_set_id
      description: Description for `objective_set_id`
    objective_group_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      x-summary: Summary for objective_group_id
      description: Description for `objective_group_id`
  example:
    $ref: ../examples/objective_set_to_objective.json
