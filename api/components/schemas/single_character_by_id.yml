single_character_by_id:
  description: Description for `single_character_by_id`.
  properties:
    character_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      format: int64
      description: Description for `character_id`
      x-reference-to:
        - character.character_id
    name:
      type: string
      description: Description for `name`
    faction_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `faction_id`
      x-reference-to:
        - faction.faction_id
    head_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `head_id`
    title_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `title_id`
      x-reference-to:
        - title.title_id
    times:
      type: string
      description: Description for `times`
    certs:
      type: string
      description: Description for `certs`
    battle_rank:
      type: string
      description: Description for `battle_rank`
    profile_id:
      type: integer
      description: Description for `profile_id`
      x-reference-to:
        - profile.profile_id
        - profile_2.profile_id
    daily_ribbon:
      type: string
      description: Description for `daily_ribbon`
    prestige_level:
      type: integer
      description: Description for `prestige_level`
    currency:
      type: string
      description: Description for `currency`
    stats:
      type: string
      description: Description for `stats`
    items:
      type: string
      description: Description for `items`
    sub_timing:
      type: boolean
      description: Description for `sub_timing`
