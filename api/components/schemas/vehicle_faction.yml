vehicle_faction:
  description: Description for `vehicle_faction`.
  properties:
    vehicle_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `vehicle_id`
      x-reference-to:
        - vehicle.vehicle_id
    faction_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `faction_id`
      x-reference-to:
        - faction.faction_id
