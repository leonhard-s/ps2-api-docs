characters_item:
  description: Description for `characters_item`.
  properties:
    character_id:
      type: integer
      format: int64
      x-summary: Summary for character_id
      description: Description for `character_id`
    item_id:
      type: integer
      x-summary: Summary for item_id
      description: Description for `item_id`
  example:
    $ref: ../examples/characters_item.json
