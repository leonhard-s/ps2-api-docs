outfit:
  description: Description for `outfit`.
  properties:
    outfit_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `outfit_id`
    name:
      type: string
      description: Description for `name`
    name_lower:
      type: string
      description: Description for `name_lower`
    alias:
      type: string
      description: Description for `alias`
    alias_lower:
      type: string
      description: Description for `alias_lower`
    time_created:
      type: integer
      description: Description for `time_created`
    time_created_date:
      type: string
      description: Description for `time_created_date`
    leader_character_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      format: int64
      description: Description for `leader_character_id`
      x-reference-to:
        - character.character_id
    member_count:
      type: integer
      description: Description for `member_count`
