currency:
  description: Description for `currency`.
  properties:
    currency_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `currency_id`
    name:
      type: string
      description: Description for `name`
    icon_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      description: Description for `icon_id`
      x-reference-to:
        - image.image_id
    inventory_cap:
      type: integer
      description: Description for `inventory_cap`
