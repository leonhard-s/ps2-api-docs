currency:
  description: Description for `currency`.
  properties:
    currency_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      x-summary: Summary for currency_id
      description: Description for `currency_id`
    name:
      type: string
      x-summary: Summary for name
      description: Description for `name`
    icon_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      x-summary: Summary for icon_id
      description: Description for `icon_id`
    inventory_cap:
      type: integer
      x-summary: Summary for inventory_cap
      description: Description for `inventory_cap`
  example:
    $ref: ../examples/currency.json
