characters_weapon_stat_by_faction:
  description: Description for `characters_weapon_stat_by_faction`.
  properties:
    character_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      format: int64
      description: Description for `character_id`
      x-reference-to:
        - character.character_id
    stat_name:
      type: string
      description: Description for `stat_name`
    item_id:
      type: integer
      description: Description for `item_id`
      x-reference-to:
        - item.item_id
    vehicle_id:
      type: integer
      description: Description for `vehicle_id`
      x-reference-to:
        - vehicle.vehicle_id
    value_vs:
      type: integer
      description: Description for `value_vs`
    value_nc:
      type: integer
      description: Description for `value_nc`
    value_tr:
      type: integer
      description: Description for `value_tr`
    last_save:
      type: integer
      description: Description for `last_save`
    last_save_date:
      type: string
      description: Description for `last_save_date`
