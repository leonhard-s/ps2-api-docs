reward_group_to_reward:
  description: Description for `reward_group_to_reward`.
  properties:
    reward_group_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      x-summary: Summary for reward_group_id
      description: Description for `reward_group_id`
    reward_id:
      type: integer
      minimum: 0
      exclusiveMinimum: true
      x-summary: Summary for reward_id
      description: Description for `reward_id`
  example:
    $ref: ../examples/reward_group_to_reward.json
