get:
  summary: Summary of characters_directive_tier
  description: Description for `characters_directive_tier`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_directive_tier_list:
                type: array
                items:
                  $ref: ../components/schemas/characters_directive_tier.yml#/characters_directive_tier
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/characters_directive_tier.json"
