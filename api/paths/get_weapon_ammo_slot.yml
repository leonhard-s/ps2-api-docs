get:
  summary: Summary of weapon_ammo_slot
  description: Description for `weapon_ammo_slot`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              weapon_ammo_slot_list:
                type: array
                items:
                  $ref: "../components/schemas/weapon_ammo_slot.yml#/weapon_ammo_slot"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/weapon_ammo_slot.json"
