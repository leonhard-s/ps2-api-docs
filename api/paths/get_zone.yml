get:
  summary: Summary of zone
  description: Description for `zone`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              zone_list:
                type: array
                items:
                  $ref: "../components/schemas/zone.yml#/zone"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/zone.json"
