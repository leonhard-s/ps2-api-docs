get:
  summary: Summary of characters_currency
  description: Description for `characters_currency`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_currency_list:
                type: array
                items:
                  $ref: "../components/schemas/characters_currency.yml#/characters_currency"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/characters_currency.json"
