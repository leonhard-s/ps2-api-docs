get:
  summary: Summary of weapon
  description: Description for `weapon`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              weapon_list:
                type: array
                items:
                  $ref: ../components/schemas/weapon.yml#/weapon
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
