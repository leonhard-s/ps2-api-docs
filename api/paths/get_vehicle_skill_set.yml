get:
  summary: Summary of vehicle_skill_set
  description: Description for `vehicle_skill_set`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              vehicle_skill_set_list:
                type: array
                items:
                  $ref: "../components/schemas/vehicle_skill_set.yml#/vehicle_skill_set"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/vehicle_skill_set.json"
