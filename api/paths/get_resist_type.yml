get:
  summary: Summary of resist_type
  description: Description for `resist_type`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              resist_type_list:
                type: array
                items:
                  $ref: "../components/schemas/resist_type.yml#/resist_type"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/resist_type.json"
