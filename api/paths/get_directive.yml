get:
  summary: Summary of directive
  description: Description for `directive`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              directive_list:
                type: array
                items:
                  $ref: "../components/schemas/directive.yml#/directive"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/directive.json"
