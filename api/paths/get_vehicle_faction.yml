get:
  summary: Summary of vehicle_faction
  description: Description for `vehicle_faction`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              vehicle_faction_list:
                type: array
                items:
                  $ref: "../components/schemas/vehicle_faction.yml#/vehicle_faction"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/vehicle_faction.json"
