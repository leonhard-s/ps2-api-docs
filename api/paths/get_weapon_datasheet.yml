get:
  summary: Summary of weapon_datasheet
  description: Description for `weapon_datasheet`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              weapon_datasheet_list:
                type: array
                items:
                  $ref: "../components/schemas/weapon_datasheet.yml#/weapon_datasheet"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/weapon_datasheet.json"
