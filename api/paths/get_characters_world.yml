get:
  summary: Summary of characters_world
  description: Description for `characters_world`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_world_list:
                type: array
                items:
                  $ref: "../components/schemas/characters_world.yml#/characters_world"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/characters_world.json"
