get:
  summary: Summary of characters_stat_by_faction
  description: Description for `characters_stat_by_faction`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_stat_by_faction_list:
                type: array
                items:
                  $ref: ../components/schemas/characters_stat_by_faction.yml#/characters_stat_by_faction
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/characters_stat_by_faction.json"
