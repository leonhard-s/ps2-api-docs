get:
  summary: Summary of vehicle_attachment
  description: Description for `vehicle_attachment`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              vehicle_attachment_list:
                type: array
                items:
                  $ref: "../components/schemas/vehicle_attachment.yml#/vehicle_attachment"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/vehicle_attachment.json"
