get:
  summary: Summary of reward_set_to_reward_group
  description: Description for `reward_set_to_reward_group`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              reward_set_to_reward_group_list:
                type: array
                items:
                  $ref: "../components/schemas/reward_set_to_reward_group.yml#/reward_set_to_reward_group"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/reward_set_to_reward_group.json"
