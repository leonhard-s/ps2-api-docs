get:
  summary: Summary of skill_line
  description: Description for `skill_line`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              skill_line_list:
                type: array
                items:
                  $ref: "../components/schemas/skill_line.yml#/skill_line"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/skill_line.json"
