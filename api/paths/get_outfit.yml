get:
  summary: Summary of outfit
  description: Description for `outfit`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              outfit_list:
                type: array
                items:
                  $ref: "../components/schemas/outfit.yml#/outfit"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/outfit.json"
