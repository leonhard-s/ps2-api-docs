get:
  summary: Summary of zone_effect
  description: Description for `zone_effect`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              zone_effect_list:
                type: array
                items:
                  $ref: ../components/schemas/zone_effect.yml#/zone_effect
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
