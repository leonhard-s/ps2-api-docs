get:
  summary: Summary of characters_online_status
  description: Description for `characters_online_status`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_online_status_list:
                type: array
                items:
                  $ref: ../components/schemas/characters_online_status.yml#/characters_online_status
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
