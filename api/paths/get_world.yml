get:
  summary: Summary of world
  description: Description for `world`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              world_list:
                type: array
                items:
                  $ref: ../components/schemas/world.yml#/world
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
