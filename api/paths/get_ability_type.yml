get:
  summary: Summary of ability_type
  description: Description for `ability_type`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              ability_type_list:
                type: array
                items:
                  $ref: ../components/schemas/ability_type.yml#/ability_type
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
