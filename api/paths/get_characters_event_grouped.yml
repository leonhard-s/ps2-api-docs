get:
  summary: Summary of characters_event_grouped
  description: Description for `characters_event_grouped`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_event_grouped_list:
                type: array
                items:
                  $ref: "../components/schemas/characters_event_grouped.yml#/characters_event_grouped"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/characters_event_grouped.json"
