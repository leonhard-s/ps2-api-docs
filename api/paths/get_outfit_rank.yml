get:
  summary: Summary of outfit_rank
  description: Description for `outfit_rank`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              outfit_rank_list:
                type: array
                items:
                  $ref: "../components/schemas/outfit_rank.yml#/outfit_rank"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/outfit_rank.json"
