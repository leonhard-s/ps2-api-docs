get:
  summary: Summary of item_profile
  description: Description for `item_profile`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              item_profile_list:
                type: array
                items:
                  $ref: "../components/schemas/item_profile.yml#/item_profile"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/item_profile.json"
