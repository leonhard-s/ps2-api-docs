get:
  summary: Summary of item_type
  description: Description for `item_type`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              item_type_list:
                type: array
                items:
                  $ref: "../components/schemas/item_type.yml#/item_type"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/item_type.json"
