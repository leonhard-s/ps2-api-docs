get:
  summary: Summary of player_state
  description: Description for `player_state`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              player_state_list:
                type: array
                items:
                  $ref: "../components/schemas/player_state.yml#/player_state"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/player_state.json"
