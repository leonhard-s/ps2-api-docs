get:
  summary: Character
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              character_list:
                type: array
                items:
                  $ref: "../components/schemas/character.yml#/character"
              results:
                type: integer
                minimum: 0
          example:
            value:
              $ref: "../components/examples/character.json"
