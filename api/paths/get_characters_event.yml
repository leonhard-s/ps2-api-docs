get:
  summary: Summary of characters_event
  description: Description for `characters_event`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_event_list:
                type: array
                items:
                  $ref: ../components/schemas/characters_event.yml#/characters_event
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/characters_event.json"
