get:
  summary: Summary of experience_rank
  description: Description for `experience_rank`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              experience_rank_list:
                type: array
                items:
                  $ref: "../components/schemas/experience_rank.yml#/experience_rank"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/experience_rank.json"
