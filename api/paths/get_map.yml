get:
  summary: Summary of map
  description: Description for `map`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              map_list:
                type: array
                items:
                  $ref: "../components/schemas/map.yml#/map"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/map.json"
