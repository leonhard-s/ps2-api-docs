get:
  summary: Summary of characters_directive
  description: Description for `characters_directive`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_directive_list:
                type: array
                items:
                  $ref: "../components/schemas/characters_directive.yml#/characters_directive"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/characters_directive.json"
