get:
  summary: Summary of characters_stat
  description: Description for `characters_stat`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_stat_list:
                type: array
                items:
                  $ref: "../components/schemas/characters_stat.yml#/characters_stat"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/characters_stat.json"
