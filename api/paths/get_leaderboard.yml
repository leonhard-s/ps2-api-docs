get:
  summary: Summary of leaderboard
  description: Description for `leaderboard`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              leaderboard_list:
                type: array
                items:
                  $ref: ../components/schemas/leaderboard.yml#/leaderboard
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
