get:
  summary: Summary of map_hex
  description: Description for `map_hex`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              map_hex_list:
                type: array
                items:
                  $ref: "../components/schemas/map_hex.yml#/map_hex"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/map_hex.json"
