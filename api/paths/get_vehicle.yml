get:
  summary: Summary of vehicle
  description: Description for `vehicle`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              vehicle_list:
                type: array
                items:
                  $ref: "../components/schemas/vehicle.yml#/vehicle"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/vehicle.json"
