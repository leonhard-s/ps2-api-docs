get:
  summary: Summary of skill_set
  description: Description for `skill_set`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              skill_set_list:
                type: array
                items:
                  $ref: "../components/schemas/skill_set.yml#/skill_set"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/skill_set.json"
