get:
  summary: Summary of reward_group_to_reward
  description: Description for `reward_group_to_reward`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              reward_group_to_reward_list:
                type: array
                items:
                  $ref: "../components/schemas/reward_group_to_reward.yml#/reward_group_to_reward"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/reward_group_to_reward.json"
