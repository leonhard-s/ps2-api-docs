get:
  summary: Summary of characters_directive_objective
  description: Description for `characters_directive_objective`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_directive_objective_list:
                type: array
                items:
                  $ref: "../components/schemas/characters_directive_objective.yml#/characters_directive_objective"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/characters_directive_objective.json"
