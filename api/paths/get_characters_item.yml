get:
  summary: Summary of characters_item
  description: Description for `characters_item`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_item_list:
                type: array
                items:
                  $ref: "../components/schemas/characters_item.yml#/characters_item"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/characters_item.json"
