get:
  summary: Summary of achievement
  description: Description for `achievement`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              achievement_list:
                type: array
                items:
                  $ref: "../components/schemas/achievement.yml#/achievement"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/achievement.json"
