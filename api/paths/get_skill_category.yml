get:
  summary: Summary of skill_category
  description: Description for `skill_category`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              skill_category_list:
                type: array
                items:
                  $ref: "../components/schemas/skill_category.yml#/skill_category"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/skill_category.json"
