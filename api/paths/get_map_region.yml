get:
  summary: Summary of map_region
  description: Description for `map_region`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              map_region_list:
                type: array
                items:
                  $ref: "../components/schemas/map_region.yml#/map_region"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/map_region.json"
