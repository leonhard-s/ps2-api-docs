get:
  summary: Summary of world_event
  description: Description for `world_event`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              world_event_list:
                type: array
                items:
                  $ref: ../components/schemas/world_event.yml#/world_event
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/world_event.json"
