get:
  summary: Summary of image_set
  description: Description for `image_set`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              image_set_list:
                type: array
                items:
                  $ref: "../components/schemas/image_set.yml#/image_set"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/image_set.json"
