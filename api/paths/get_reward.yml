get:
  summary: Summary of reward
  description: Description for `reward`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              reward_list:
                type: array
                items:
                  $ref: "../components/schemas/reward.yml#/reward"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/reward.json"
