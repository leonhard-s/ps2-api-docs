get:
  summary: Summary of empire_scores
  description: Description for `empire_scores`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              empire_scores_list:
                type: array
                items:
                  $ref: "../components/schemas/empire_scores.yml#/empire_scores"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/empire_scores.json"
