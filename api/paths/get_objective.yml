get:
  summary: Summary of objective
  description: Description for `objective`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              objective_list:
                type: array
                items:
                  $ref: "../components/schemas/objective.yml#/objective"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/objective.json"
