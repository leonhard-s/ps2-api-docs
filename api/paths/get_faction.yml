get:
  summary: Summary of faction
  description: Description for `faction`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              faction_list:
                type: array
                items:
                  $ref: ../components/schemas/faction.yml#/faction
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
