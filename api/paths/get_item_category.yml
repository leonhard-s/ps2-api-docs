get:
  summary: Summary of item_category
  description: Description for `item_category`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              item_category_list:
                type: array
                items:
                  $ref: "../components/schemas/item_category.yml#/item_category"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/item_category.json"
