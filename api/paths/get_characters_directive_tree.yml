get:
  summary: Summary of characters_directive_tree
  description: Description for `characters_directive_tree`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_directive_tree_list:
                type: array
                items:
                  $ref: ../components/schemas/characters_directive_tree.yml#/characters_directive_tree
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
