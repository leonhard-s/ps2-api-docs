get:
  summary: Summary of image_set_default
  description: Description for `image_set_default`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              image_set_default_list:
                type: array
                items:
                  $ref: "../components/schemas/image_set_default.yml#/image_set_default"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/image_set_default.json"
