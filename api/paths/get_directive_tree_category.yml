get:
  summary: Summary of directive_tree_category
  description: Description for `directive_tree_category`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              directive_tree_category_list:
                type: array
                items:
                  $ref: "../components/schemas/directive_tree_category.yml#/directive_tree_category"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/directive_tree_category.json"
