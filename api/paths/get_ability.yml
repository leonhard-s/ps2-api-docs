get:
  summary: Summary of ability
  description: Description for `ability`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              ability_list:
                type: array
                items:
                  $ref: "../components/schemas/ability.yml#/ability"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/ability.json"
