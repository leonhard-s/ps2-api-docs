get:
  summary: Summary of characters_skill
  description: Description for `characters_skill`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_skill_list:
                type: array
                items:
                  $ref: "../components/schemas/characters_skill.yml#/characters_skill"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/characters_skill.json"
