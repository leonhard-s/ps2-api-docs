get:
  summary: Summary of region
  description: Description for `region`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              region_list:
                type: array
                items:
                  $ref: ../components/schemas/region.yml#/region
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
