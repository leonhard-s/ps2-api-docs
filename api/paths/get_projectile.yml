get:
  summary: Summary of projectile
  description: Description for `projectile`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              projectile_list:
                type: array
                items:
                  $ref: "../components/schemas/projectile.yml#/projectile"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/projectile.json"
