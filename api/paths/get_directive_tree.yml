get:
  summary: Summary of directive_tree
  description: Description for `directive_tree`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              directive_tree_list:
                type: array
                items:
                  $ref: "../components/schemas/directive_tree.yml#/directive_tree"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/directive_tree.json"
