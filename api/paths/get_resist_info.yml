get:
  summary: Summary of resist_info
  description: Description for `resist_info`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              resist_info_list:
                type: array
                items:
                  $ref: "../components/schemas/resist_info.yml#/resist_info"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/resist_info.json"
