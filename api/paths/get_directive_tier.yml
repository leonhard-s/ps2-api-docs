get:
  summary: Summary of directive_tier
  description: Description for `directive_tier`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              directive_tier_list:
                type: array
                items:
                  $ref: "../components/schemas/directive_tier.yml#/directive_tier"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/directive_tier.json"
