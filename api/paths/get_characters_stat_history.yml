get:
  summary: Summary of characters_stat_history
  description: Description for `characters_stat_history`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_stat_history_list:
                type: array
                items:
                  $ref: "../components/schemas/characters_stat_history.yml#/characters_stat_history"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          example:
            value:
              $ref: "../components/examples/characters_stat_history.json"
