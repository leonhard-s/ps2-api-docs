get:
  summary: Summary of armor_info
  description: Description for `armor_info`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              armor_info_list:
                type: array
                items:
                  $ref: "../components/schemas/armor_info.yml#/armor_info"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/armor_info.json"
