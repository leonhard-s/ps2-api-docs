get:
  summary: Summary of characters_friend
  description: Description for `characters_friend`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_friend_list:
                type: array
                items:
                  $ref: "../components/schemas/characters_friend.yml#/characters_friend"
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/characters_friend.json"
