get:
  summary: Summary of reward_type
  description: Description for `reward_type`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              reward_type_list:
                type: array
                items:
                  $ref: "../components/schemas/reward_type.yml#/reward_type"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/reward_type.json"
