get:
  summary: Summary of character_name
  description: Description for `character_name`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              character_name_list:
                type: array
                items:
                  $ref: "../components/schemas/character_name.yml#/character_name"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/character_name.json"
