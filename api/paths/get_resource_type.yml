get:
  summary: Summary of resource_type
  description: Description for `resource_type`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              resource_type_list:
                type: array
                items:
                  $ref: "../components/schemas/resource_type.yml#/resource_type"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/resource_type.json"
