get:
  summary: Summary of image
  description: Description for `image`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              image_list:
                type: array
                items:
                  $ref: ../components/schemas/image.yml#/image
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
