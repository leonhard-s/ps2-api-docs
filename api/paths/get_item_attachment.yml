get:
  summary: Summary of item_attachment
  description: Description for `item_attachment`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              item_attachment_list:
                type: array
                items:
                  $ref: "../components/schemas/item_attachment.yml#/item_attachment"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/item_attachment.json"
