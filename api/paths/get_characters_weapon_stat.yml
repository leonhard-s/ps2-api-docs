get:
  summary: Summary of characters_weapon_stat
  description: Description for `characters_weapon_stat`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              characters_weapon_stat_list:
                type: array
                items:
                  $ref: ../components/schemas/characters_weapon_stat.yml#/characters_weapon_stat
              results:
                type: integer
                minimum: 0
                description: The number of results returned.
