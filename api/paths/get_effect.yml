get:
  summary: Summary of effect
  description: Description for `effect`
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              effect_list:
                type: array
                items:
                  $ref: "../components/schemas/effect.yml#/effect"
                required: true
              results:
                type: integer
                required: true
                minimum: 0
                description: The number of results returned.
          examples:
            value:
              $ref: "../components/examples/effect.json"
