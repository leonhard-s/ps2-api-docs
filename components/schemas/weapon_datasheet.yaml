type: object
description: >
  **Deprecated:** This table is not directly tied to the underlying fire mode and player
  state group data and may differ wildly from the true values!


  Suggested alternatives for the values provided by this collection are listed as part of the
  `weapon_datasheet` schema.
x-tags: ["Weapons & Firing Mechanics"]
properties:
  capacity:
    description: Unreliable! Please refer to the matching `ps2/weapon_ammo_slot` instead.
    type: string
    x-cast-to: int32
  clip_size:
    description: Unreliable! Please refer to the matching `ps2/weapon_ammo_slot` instead.
    type: string
    x-cast-to: int32
  damage:
    description: Unreliable! Please use the values from `ps2/fire_mode_2` instead.
    type: string
    x-cast-to: int32
  damage_max:
    description: Unreliable! Please use the values from `ps2/fire_mode_2` instead.
    type: string
    x-cast-to: int32
  damage_min:
    description: Unreliable! Please use the values from `ps2/fire_mode_2` instead.
    type: string
    x-cast-to: int32
  direct_damage:
    description:
      Unreliable! Please refer to the damage effect listed in `ps2/fire_mode_2.damage_direct_effect_id`
      instead.
    type: string
    x-cast-to: int32
  fire_cone:
    description:
      Unreliable! Please refer to the player state group listed in `ps2/fire_mode_2.player_state_group_id`
      instead.
    type: string
    x-cast-to: int32
  fire_cone_max:
    description:
      Unreliable! Please refer to the `cof_` values in `ps2/fire_mode_2`
      instead.
    type: string
    x-cast-to: int32
  fire_cone_min:
    description:
      Unreliable! Please refer to the player state group listed in `ps2/fire_mode_2.player_state_group_id`
      instead.
    type: string
    x-cast-to: int32
  fire_rate_ms:
    description: Unreliable! Please use `ps2/fire_mode_2.fire_refire_ms` instead.
    type: string
    x-cast-to: int32
  fire_rate_mx_max:
    description: Unreliable! Please use `ps2/fire_mode_2.fire_refire_ms` instead. Note that this is not a typo in the spec, the field is named `mx`.
    type: string
    x-cast-to: int32
  fire_rate_ms_min:
    description: Unreliable! Please use `ps2/fire_mode_2.fire_refire_ms` instead.
    type: string
    x-cast-to: int32
  indirect_damage:
    description:
      Unreliable! Please refer to the damage effect listed in `ps2/fire_mode_2.damage_indirect_effect_id`
      instead.
    type: string
    x-cast-to: int32
  item_id:
    description: ID of the item to which this datasheet belongs.
    type: string
    x-cast-to: int32
  range:
    $ref: "localized_string.yaml"
    description: Localized name of the displayed range of the weapon.
  reload_ms:
    description:
      Unreliable! Please add the values of `ps2/fire_mode_2.reload_time_ms`
      instead.
    type: string
    x-cast-to: int32
  reload_ms_max:
    description:
      Unreliable! Please add the values of `ps2/fire_mode_2.reload_time_ms`
      and `ps2/fire_mode_2.reload_chamber_ms` together to get long reload time.
    type: string
    x-cast-to: int32
  reload_ms_min:
    description:
      Unreliable! Please add the values of `ps2/fire_mode_2.reload_time_ms`
      instead.
    type: string
    x-cast-to: int32
  show_clip_size:
    description: Whether the clip size should be displayed in the UI.
    type: string
    x-cast-to: boolean
  show_fire_modes:
    description:
      Whether the fire modes (in-game interpretation of this term) should
      be displayed in the UI.
    type: string
    x-cast-to: boolean
  show_range:
    description: Whether the range string should be displayed in the UI.
    type: string
    x-cast-to: boolean
required:
  - item_id
examples:
  - $ref: "../../examples/weapon_datasheet.json"
