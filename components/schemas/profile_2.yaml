type: object
description: >
  Extended profile endpoint containing all entity types, including NPCs, construction items,
  and deployables.
x-tags: ["Fundamentals"]
properties:
  description:
    description: Internal description of this profile. Not localized or user-facing.
    type: string
  profile_id:
    description: Unique ID of the collection. Note that this is one of the few collections
      where the ID field is not `<collection>_id`.
    type: string
    x-cast-to: int32
examples:
  - $ref: "../../examples/profile_2.json"
