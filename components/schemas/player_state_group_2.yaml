x-tags: ["Weapons & Firing Mechanics"]
properties:
  player_state_group_id:
    description: Unique ID of the player state group.
    type: string
    x-cast-to: int32
  player_state_id:
    description: Player state ID enum value.
    type: string
    x-cast-to: int32
    x-reference-to: player_state.player_state_id
  can_iron_sight:
    description: Whether the player is allowed to enter aim-down-sights (ADS) in this state (0 or 1).
    type: string
    x-cast-to: bool
  cof_grow_rate:
    description: Cone of fire growth per shot.
    type: string
    x-cast-to: float
  cof_max:
    description: Maximum cone of fire.
    type: string
    x-cast-to: float
  cof_min:
    description: Minimum cone of fire.
    type: string
    x-cast-to: float
  cof_recovery_delay_ms:
    description: Time before the cone-of-fire will start to reset after the user stopped firing.
    type: string
    x-cast-to: int32
  cof_recovery_rate:
    description: CoThe speed at which the cone-of-fire will return to its starting value after the user stopped firing.
    type: string
    x-cast-to: float
  cof_shots_before_penalty:
    description: Number of "free shots" before cone of fire penalty is applied.
    type: string
    x-cast-to: int32
  cof_recovery_delay_threshold:
    description: "(Not yet documented)"
    x-cast-to: int32
  cof_turn_penalty:
    description: "(Not yet documented)"
    type: string
    x-cast-to: float
type: object
required:
  - player_state_group_id
  - player_state_id
