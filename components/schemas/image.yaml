properties:
  description:
    description: Description of the asset.
    type: string
  image_id:
    description: Unique ID of the image.
    type: string
    x-cast-to: int32
  path:
    description: Relative to the image asset. Prefix the base URL to the Census API
      endpoint to create a fully-qualified URL.
    type: string
type: object
