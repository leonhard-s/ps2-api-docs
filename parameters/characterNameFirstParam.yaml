description: Name of the character. Note that when looking up characters, it is preferable
  to query `name.first_lower` with a lowercase version of the search string.
examples:
  higby:
    summary: Higby
    value: Higby
  notSet:
    summary: Unset
    value: ''
in: query
name: name.first
schema:
  type: string
