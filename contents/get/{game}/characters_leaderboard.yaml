schema:
  properties:
    characters_leaderboard_list:
      items:
        $ref: ../../../schemas/leaderboard.yaml
      type: array
    count:
      minimum: 0
      type: integer
    key:
      type: string
    limit:
      minimum: 0
      type: integer
    returned:
      minimum: 0
      type: integer
  type: object
