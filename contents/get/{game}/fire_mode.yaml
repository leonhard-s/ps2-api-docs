examples:
  fire_mode:
    value:
      $ref: ../../../examples/fire_mode.json
schema:
  properties:
    fire_mode_list:
      items:
        $ref: ../../../schemas/fire_mode.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
