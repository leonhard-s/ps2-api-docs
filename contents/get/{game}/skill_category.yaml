examples:
  skill_category:
    value:
      $ref: ../../../examples/skill_category.json
schema:
  properties:
    returned:
      minimum: 0
      type: integer
    skill_category_list:
      items:
        $ref: ../../../components/schemas/skill_category.yaml
      type: array
  type: object
