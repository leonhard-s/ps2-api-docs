examples:
  reward:
    value:
      $ref: ../../../examples/reward.json
schema:
  properties:
    returned:
      minimum: 0
      type: integer
    reward_list:
      items:
        $ref: ../../../components/schemas/reward.yaml
      type: array
  type: object
