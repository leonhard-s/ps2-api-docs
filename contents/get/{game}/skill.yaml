examples:
  skill:
    value:
      $ref: ../../../examples/skill.json
schema:
  properties:
    returned:
      minimum: 0
      type: integer
    skill_list:
      items:
        $ref: ../../../schemas/skill.yaml
      type: array
  type: object
