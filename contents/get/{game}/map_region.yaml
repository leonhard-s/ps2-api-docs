examples:
  indar:
    value:
      $ref: ../../../examples/map_region.json
schema:
  properties:
    map_region_list:
      items:
        $ref: ../../../components/schemas/map_region.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
