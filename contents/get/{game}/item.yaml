examples:
  knife:
    value:
      $ref: ../../../examples/item.json
schema:
  properties:
    item_list:
      items:
        $ref: ../../../components/schemas/item.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
