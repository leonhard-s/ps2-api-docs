examples:
  capturePoint:
    value:
      $ref: ../../../examples/objective_type.json
schema:
  properties:
    objective_type_list:
      items:
        $ref: ../../../schemas/objective_type.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
