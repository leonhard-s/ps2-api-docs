examples:
  zone_effect:
    value:
      $ref: ../../../examples/zone_effect.json
schema:
  properties:
    returned:
      minimum: 0
      type: integer
    zone_effect_list:
      items:
        $ref: ../../../schemas/zone_effect.yaml
      type: array
  type: object
