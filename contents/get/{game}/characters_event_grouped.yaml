schema:
  properties:
    characters_event_grouped_list:
      items:
        $ref: ../../../components/schemas/characters_event_grouped.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
