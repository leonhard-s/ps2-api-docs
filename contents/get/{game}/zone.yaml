examples:
  indar:
    value:
      $ref: ../../../examples/zone.json
schema:
  properties:
    returned:
      minimum: 0
      type: integer
    zone_list:
      items:
        $ref: ../../../components/schemas/zone.yaml
      type: array
  type: object
