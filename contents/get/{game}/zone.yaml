examples:
  indar:
    value:
      $ref: ../../../examples/zone.json
schema:
  properties:
    returned:
      minimum: 0
      type: integer
    zone_list:
      items:
        $ref: ../../../schemas/zone.yaml
      type: array
  type: object
