examples:
  trLightAssault:
    value:
      $ref: ../../../examples/profile.json
schema:
  properties:
    profile_list:
      items:
        $ref: ../../../components/schemas/profile.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
