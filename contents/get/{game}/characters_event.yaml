schema:
  properties:
    characters_event_list:
      items:
        $ref: ../../../components/schemas/characters_event.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
