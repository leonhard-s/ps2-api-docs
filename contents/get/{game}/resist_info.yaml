examples:
  resist_info:
    value:
      $ref: ../../../examples/resist_info.json
schema:
  properties:
    resist_info_list:
      items:
        $ref: ../../../schemas/resist_info.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
