examples:
  fno:
    value:
      $ref: ../../../examples/outfit.json
schema:
  properties:
    outfit_list:
      items:
        $ref: ../../../schemas/outfit.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
