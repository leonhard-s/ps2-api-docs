examples:
  higby:
    value:
      $ref: ../../../examples/characters_stat.json
schema:
  properties:
    characters_stat_list:
      items:
        $ref: ../../../schemas/characters_stat.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
