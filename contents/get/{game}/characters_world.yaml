examples:
  higby:
    value:
      $ref: ../../../examples/characters_world.json
schema:
  properties:
    characters_world_list:
      items:
        $ref: ../../../components/schemas/characters_world.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
