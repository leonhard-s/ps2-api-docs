examples:
  ns15mBundle:
    value:
      $ref: ../../../examples/objective.json
schema:
  properties:
    objective_list:
      items:
        $ref: ../../../components/schemas/objective.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
