examples:
  higby:
    value:
      $ref: ../../../examples/character_higby.json
schema:
  properties:
    character_list:
      items:
        $ref: ../../../schemas/character.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
