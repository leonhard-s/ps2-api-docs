examples:
  higby:
    value:
      $ref: ../../../examples/empire_scores.json
schema:
  properties:
    empire_scores_list:
      items:
        $ref: ../../../schemas/empire_scores.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
