examples:
  nanites:
    value:
      $ref: ../../../examples/currency.json
schema:
  properties:
    currency_list:
      items:
        $ref: ../../../schemas/currency.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
