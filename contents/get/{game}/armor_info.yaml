examples:
  armor_info:
    value:
      $ref: ../../../examples/armor_info.json
schema:
  properties:
    armor_info_list:
      items:
        $ref: ../../../schemas/armor_info.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
