examples:
  indar:
    value:
      $ref: ../../../examples/map.json
schema:
  properties:
    map_list:
      items:
        $ref: ../../../components/schemas/map.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
