examples:
  image:
    value:
      $ref: ../../../examples/image.json
schema:
  properties:
    image_list:
      items:
        $ref: ../../../schemas/image.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
