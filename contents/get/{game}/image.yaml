examples:
  image:
    value:
      $ref: ../../../examples/image.json
schema:
  properties:
    image_list:
      items:
        $ref: ../../../components/schemas/image.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
