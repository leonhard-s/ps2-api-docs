examples:
  knife:
    value:
      $ref: ../../../examples/item_profile.json
schema:
  properties:
    item_profile_list:
      items:
        $ref: ../../../schemas/item_profile.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
