examples:
  connery:
    value:
      $ref: ../../../examples/world.json
schema:
  properties:
    returned:
      minimum: 0
      type: integer
    world_list:
      items:
        $ref: ../../../components/schemas/world.yaml
      type: array
  type: object
