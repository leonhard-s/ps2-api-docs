examples:
  harasser:
    value:
      $ref: ../../../examples/vehicle.json
schema:
  properties:
    returned:
      minimum: 0
      type: integer
    vehicle_list:
      items:
        $ref: ../../../components/schemas/vehicle.yaml
      type: array
  type: object
