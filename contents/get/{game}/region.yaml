examples:
  region:
    value:
      $ref: ../../../examples/region.json
schema:
  properties:
    region_list:
      items:
        $ref: ../../../components/schemas/region.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
