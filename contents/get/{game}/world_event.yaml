schema:
  properties:
    returned:
      minimum: 0
      type: integer
    world_event_list:
      items:
        $ref: ../../../components/schemas/world_event.yaml
      type: array
  type: object
