examples:
  ability:
    value:
      $ref: ../../../examples/ability.json
schema:
  properties:
    ability_list:
      items:
        $ref: ../../../components/schemas/ability.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
