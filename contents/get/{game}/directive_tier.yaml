examples:
  higby:
    value:
      $ref: ../../../examples/directive_tier.json
schema:
  properties:
    directive_tier_list:
      items:
        $ref: ../../../components/schemas/directive_tier.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
