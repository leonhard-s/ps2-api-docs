examples:
  title:
    value:
      $ref: ../../../examples/title.json
schema:
  properties:
    returned:
      minimum: 0
      type: integer
    title_list:
      items:
        $ref: ../../../components/schemas/title.yaml
      type: array
  type: object
