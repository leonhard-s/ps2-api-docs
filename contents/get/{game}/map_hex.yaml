examples:
  indar:
    value:
      $ref: ../../../examples/map_hex.json
schema:
  properties:
    map_hex_list:
      items:
        $ref: ../../../components/schemas/map_hex.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
