examples:
  cobalt_all_time_kills:
    value:
      $ref: ../../../examples/leaderboard.json
schema:
  properties:
    count:
      minimum: 0
      type: integer
    key:
      type: string
    leaderboard_list:
      items:
        $ref: ../../../schemas/leaderboard.yaml
      type: array
    returned:
      minimum: 0
      type: integer
  type: object
